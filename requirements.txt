# Pinned top-level environment for this release candidate.
# This is stronger than the compatibility envelope but is not a full transitive hash lockfile.
numpy==2.3.5
pandas==2.2.3
scikit-learn==1.8.0
qiskit==2.3.1
