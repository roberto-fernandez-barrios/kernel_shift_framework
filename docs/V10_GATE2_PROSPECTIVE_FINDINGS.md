# v1.0 finding: prospective transfer of the Gate-2 certificate

## Locked chronology

- Specification frozen: 2026-08-01, before task acquisition, model
  prediction, or target-label inspection.
- Specification SHA-256:
  `3a8318d92d4af2aeeaf0c0edb069c3be59f31da6d1ee50fb6a6256e9d9d280b0`.
- Aggregate prediction-lock SHA-256:
  `e4ee6bb89bc78047e7c848a0db3a6255cf0a2600a21b42fbab956a0e889507d1`.
- The aggregate lock contains ten complete task-seed units and five
  pre-model technical-failure records. Target labels were opened only after
  the aggregate lock was written and hashed.

## Prospective outcome

The exact frozen category is:

> **Strong prospective transfer - technically limited two-task replication.**

Against the full 115-candidate classical family at `tau=0.010`:

- 11/20 task-seed-classifier cells already cross with zero labels;
- adaptive audit counts span 0--76 labels, with overall median 0;
- task-classifier medians are 0, 0, 3, and 3 labels;
- every task-classifier median is below the 50-label criterion;
- no seed exceeds the 100-label ceiling;
- all 20 realized accuracy advantages are negative, from -0.156 to -0.002.

Dynamic random-active and non-adaptive initial-coverage controls lead to the
same scientific interpretation. Against each, adaptive coverage wins, ties,
and loses in 3, 15, and 2 cells, and its median paired difference is zero.
The transferred result belongs to the sharp certificate and
actionable-disagreement restriction, not to acquisition-policy superiority.

## Technical availability

BRFSS Diabetes and ACS Food Stamps pass all gates for five seeds each. NHANES
Lead produces 11 non-constant post-encoding features in every seed, below the
frozen `d=12` grid requirement. Minimum-feature failure was explicitly
prespecified as a technical-unavailability gate. No NHANES model candidate
was executed, and the task was neither replaced nor repaired by changing the
dimension grid.

All prospectively selected quantum winners are product maps. The prospective
experiment therefore validates transfer of Gate 2 on its two eligible fixed
tasks but does not independently replicate the retrospective entangling-ZZ
finding.

## Allowed headline

> After every prediction was locked and hashed before target-label opening,
> the sharp Gate-2 audit met its prespecified strong-transfer rule on both
> technically eligible external tasks: all four task-classifier medians
> required at most three of 500 labels to rule out more than one point of
> accuracy advantage against 115 classical witnesses.

Always append the scope qualification that a third frozen task failed its
minimum-feature gate before model execution and that the result is a fixed
two-task replication, not task-population inference.
